#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    int TC = 1;
    while (cin >> n && n) {
        cout << "Case " << TC++ << ": " << (n >> 1) << endl;
    }   
    return 0;
}